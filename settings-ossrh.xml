<settings>
    <servers>
        <server>
            <id>ossrh</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <username>${OSSRH_USERNAME}</username>
            <password>${OSSRH_PASSWORD}</password>
        </server>
    </servers>
    <profiles>
        <profile>
            <id>ossrh</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <gpg.keyname>${GPG_KEY_NAME}</gpg.keyname>
                <gpg.secretKeyring>/home/runner/.gnupg/gpg.key</gpg.secretKeyring>
                <gpg.publicKeyring>/home/runner/.gnupg/gpg.pub</gpg.publicKeyring>
                <gpg.passphrase>${GPG_PASSPHRASE}}</gpg.passphrase>
                <gpg.homedir>/home/runner/.gnupg</gpg.homedir>
            </properties>
        </profile>
    </profiles>
    <activeProfiles/>
</settings>
